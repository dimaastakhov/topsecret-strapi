{
  "routes": [
    {
      "method": "GET",
      "path": "/archives",
      "handler": "archives.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/archives/count",
      "handler": "archives.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/archives/:id",
      "handler": "archives.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/archives",
      "handler": "archives.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/archives/:id",
      "handler": "archives.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/archives/:id",
      "handler": "archives.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
